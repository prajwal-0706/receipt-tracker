fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
psycopg[binary]>=3.1
pydantic>=2.5
python-multipart>=0.0.9
python-dotenv>=1.0
pillow>=10.0,<12  # 12.x has a broken _typing import that breaks transformers image pipelines
opencv-python>=4.9
sqlglot>=23.0
httpx>=0.27
pytest>=8.0

# Model libs (only needed in the Colab runtime; safe to skip when running with stubs locally)
# transformers 5.x switched the default AWQ backend to gptqmodel, whose ExLlamaV2 kernel
# rejects Qwen2.5-VL's vision-encoder layer (out_features=3420, not /32). Pin to 4.x.
transformers>=4.49,<5.0
tokenizers>=0.21,<0.22
accelerate>=0.34
autoawq>=0.2.7
torch>=2.1
bitsandbytes>=0.43  # fallback path if AWQ kernel still misbehaves

# Tunneling (Colab only)
pyngrok>=7.0
